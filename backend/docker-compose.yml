version: "3.9"

services:
  promptshield-backend:
    build: .
    container_name: promptshield-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
